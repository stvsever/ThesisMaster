PHOENIX Step-03 target identification request.

Use the full profile evidence bundle below to produce:
1) A ranked predictor list.
2) A recommended subset of treatment targets for immediate intervention design.
3) Clear constraints and confidence.
4) Explicit breadth-first reasoning notes (domain coverage before subtree deepening).
5) If critic_feedback is present, revise the previous candidate while preserving validated strengths.

Return only schema-valid JSON.

Evidence bundle:
$EVIDENCE_BUNDLE_JSON
